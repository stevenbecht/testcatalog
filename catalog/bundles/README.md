# Bundles

This directory is intentionally present so the test repository matches the same
high-level layout as the main catalog.

Published bundle discovery and materialization are not required for the current
public Git sync smoke test, so no example `bundle.yaml` files are included here.
